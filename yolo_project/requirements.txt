# YOLOv8 项目依赖
# 安装命令: pip install -r requirements.txt

# 核心依赖
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0

# 图像处理
opencv-python>=4.8.0
Pillow>=9.0.0
numpy>=1.24.0

# 数据处理和可视化
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0

# 配置和工具
PyYAML>=6.0
requests>=2.31.0

# 可选依赖 (用于导出特定格式)
# onnx>=1.14.0              # ONNX导出
# onnxruntime>=1.15.0       # ONNX推理
# openvino>=2023.0.0        # OpenVINO导出
# tensorrt>=8.6.0           # TensorRT导出 (需要CUDA)
# coremltools>=7.0          # CoreML导出 (仅macOS)
# tensorflow>=2.13.0        # TFLite/TFJS导出
# paddlepaddle>=2.5.0       # PaddlePaddle导出

# 开发工具 (可选)
# jupyter>=1.0.0
# ipython>=8.14.0